# If you prefer the allow list template instead of the deny list, see community template:
# https://github.com/github/gitignore/blob/main/community/Golang/Go.AllowList.gitignore
#
# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work
go.work.sum

# iOS / Xcode build artifacts
**/DerivedData/
**/xcuserdata/
**/*.xcuserstate
**/*.xccheckout
**/*.xcscmblueprint

# Xcode project workspace artifacts (auto-generated inside *.xcodeproj)
**/*.xcodeproj/project.xcworkspace/
**/*.xcodeproj/project.xcworkspace/**/*

# iOS outputs
**/*.ipa
**/*.dSYM
**/*.dSYM.zip
**/*.xcarchive

# Generated XCFrameworks (project-local)
**/*.xcframework

# Android / Gradle build state
/android_module/.gradle/
/android_module/**/build/

# Lint reports (generated by `task report` / `make report`)
/reports/

# Local toolchains
/.local-tools/

# Python virtual environments
**/.venv/
**/venv/
**/__pycache__/
*.pyc

# Public desktop_build.py and the native packager generate these local staging trees.
# They are reproducible build outputs, never application source.
/output/

# Owner-local Harness/runtime worktrees and qualification artefacts.
# These are private generated state, never application source.
/runtime/

# Module-local qualification runs must stay owner-only Harness evidence.
# They are never application source or stageable public-repository files.
/go_module/runtime/
