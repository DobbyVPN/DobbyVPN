name: Build TrustTunnel bridge

env:
  CONAN_VER: 2.12

on:
  workflow_dispatch:
  push:
    paths:
      - 'go_client/trusttunnel/internal/dobby_bridge/**'

jobs:
  Linux:
    runs-on: ubuntu-22.04
    env:
      LLVM_MAJOR_VER: 21
    steps:
      - name: Checkout Code with Submodules
        uses: actions/checkout@v4
        with:
          submodules: recursive # CRITICAL: Fetches the TrustTunnel vendor code

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: Install LLVM and Dependencies
        run: |
          sudo apt install -y ninja-build
          pip install conan~=$CONAN_VER
          curl -O https://apt.llvm.org/llvm.sh
          chmod +x llvm.sh
          sudo ./llvm.sh $LLVM_MAJOR_VER
          sudo apt install -y libc++-${LLVM_MAJOR_VER}-dev libclang-rt-${LLVM_MAJOR_VER}-dev libc++abi-${LLVM_MAJOR_VER}-dev
          echo "/usr/lib/llvm-$LLVM_MAJOR_VER/bin" >> $GITHUB_PATH

      - name: Bootstrap Conan
        working-directory: TrustTunnelClient
        run: python ./scripts/bootstrap_conan_deps.py

      - name: Configure and Build Bridge
        working-directory: TrustTunnelClient/platform/windows
        shell: bash
        run: |
          # 1. Dynamically patch their CMake file to suppress the macro warning
          sed -i 's/-Wall -Wextra -Wpedantic/-Wno-gnu-zero-variadic-macro-arguments/g' CMakeLists.txt

          # 2. Inject our bridge into their tree
          echo 'add_subdirectory("../../../go_client/trusttunnel/internal/dobby_bridge" "dobby_bridge_build")' >> CMakeLists.txt
          
          mkdir build && cd build
          cmake -DCMAKE_BUILD_TYPE=RelWithDebInfo \
            -DCMAKE_C_COMPILER="clang" \
            -DCMAKE_CXX_COMPILER="clang++" \
            -DCMAKE_CXX_FLAGS="-stdlib=libc++" \
            -DCMAKE_POSITION_INDEPENDENT_CODE=ON \
            -G "Ninja" ..
          cmake --build . --target dobby_bridge

      - name: Upload Linux Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dobby_bridge-linux
          path: TrustTunnelClient/platform/windows/build/dobby_bridge_build/libdobby_bridge.so

# -------------------------------------------------------------

  MacOS:
    runs-on: macos-latest
    steps:
      - name: Checkout Code with Submodules
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: Install LLVM and Dependencies
        run: |
          brew install ninja llvm@21
          sed -i '' 's/_LIBCPP_INTRODUCED_IN_LLVM_21 1/_LIBCPP_INTRODUCED_IN_LLVM_21 0/g' "$(brew --prefix llvm@21)/include/c++/v1/__configuration/availability.h"
          pip install conan~=$CONAN_VER
          echo "/opt/homebrew/opt/llvm@21/bin" >> $GITHUB_PATH

      - name: Bootstrap Conan
        working-directory: TrustTunnelClient
        run: python ./scripts/bootstrap_conan_deps.py

      - name: Configure and Build Bridge
        working-directory: TrustTunnelClient/platform/windows
        shell: bash
        run: |
          # 1. Dynamically patch their CMake file (MacOS BSD sed syntax)
          sed -i '' 's/-Wall -Wextra -Wpedantic/-Wno-gnu-zero-variadic-macro-arguments/g' CMakeLists.txt

          # 2. Inject our bridge into their tree
          echo 'add_subdirectory("../../../go_client/trusttunnel/internal/dobby_bridge" "dobby_bridge_build")' >> CMakeLists.txt
          
          mkdir build && cd build
          cmake -DCMAKE_BUILD_TYPE=RelWithDebInfo \
            -DCMAKE_C_COMPILER="clang" \
            -DCMAKE_CXX_COMPILER="clang++" \
            -DCMAKE_CXX_FLAGS="-stdlib=libc++" \
            -DCMAKE_POSITION_INDEPENDENT_CODE=ON \
            -G "Ninja" ..
          cmake --build . --target dobby_bridge

      - name: Upload MacOS Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dobby_bridge-macos
          path: TrustTunnelClient/platform/windows/build/dobby_bridge_build/libdobby_bridge.dylib

# -------------------------------------------------------------

  Windows:
    runs-on: windows-latest
    env:
      MSVC_VER: 17
      MSVC_YEAR: 2022
    if: "!startsWith(github.event.head_commit.message, 'skipci:')"
    steps:
      - name: Checkout Code with Submodules
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: Set up MSVC
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: x64

      - name: Install Dependencies
        run: |
          choco install strawberryperl nasm
          pip install conan~=${{ env.CONAN_VER }} ninja

      - name: Bootstrap Conan
        working-directory: TrustTunnelClient
        run: python ./scripts/bootstrap_conan_deps.py

      - name: Configure and Build Bridge
        working-directory: TrustTunnelClient/platform/windows
        run: |
          Add-Content -Path CMakeLists.txt -Value 'add_subdirectory("../../../go_client/trusttunnel/internal/dobby_bridge" "dobby_bridge_build")'
          mkdir build
          cd build
          cmake -DCMAKE_BUILD_TYPE=RelWithDebInfo `
            -DCMAKE_C_COMPILER="cl.exe" `
            -DCMAKE_CXX_COMPILER="cl.exe" `
            -G "Ninja" ..
          cmake --build . --target dobby_bridge

      - name: Upload Windows Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dobby_bridge-windows
          path: |
            TrustTunnelClient\platform\windows\build\dobby_bridge_build\dobby_bridge.dll
            TrustTunnelClient\platform\windows\build\dobby_bridge_build\dobby_bridge.lib

  Android-ARM64:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout Code with Submodules
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - name: Install Rust Toolchains for Android
        run: |
          rustup target add aarch64-linux-android
          cargo install cargo-ndk
      - name: Install Dependencies
        run: |
          sudo apt install -y ninja-build
          pip install conan~=$CONAN_VER

      - name: Bootstrap Conan
        working-directory: TrustTunnelClient
        run: python ./scripts/bootstrap_conan_deps.py

      - name: Configure and Build Bridge (Android)
        working-directory: TrustTunnelClient/platform/windows
        shell: bash
        run: |
          # 1. Inject warning suppressions
          sed -i 's/-Wall -Wextra -Wpedantic/-Wno-gnu-zero-variadic-macro-arguments/g' CMakeLists.txt
          
          # 2. Inject into their tree
          echo 'add_subdirectory("../../../go_client/trusttunnel/internal/dobby_bridge" "dobby_bridge_build")' >> CMakeLists.txt
          
          mkdir build && cd build
          
          # 4. Compile using the Android NDK Toolchain
          cmake -DCMAKE_TOOLCHAIN_FILE=$ANDROID_NDK_LATEST_HOME/build/cmake/android.toolchain.cmake \
            -DANDROID_ABI=arm64-v8a \
            -DANDROID_PLATFORM=android-24 \
            -DCMAKE_BUILD_TYPE=RelWithDebInfo \
            -G "Ninja" ..
            
          cmake --build . --target dobby_bridge

      - name: Upload Android Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dobby_bridge-android-arm64
          path: TrustTunnelClient/platform/windows/build/dobby_bridge_build/libdobby_bridge.so

# -------------------------------------------------------------

  iOS-ARM64:
    runs-on: macos-latest
    steps:
      - name: Checkout Code with Submodules
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: Install Dependencies
        run: |
          brew install ninja
          pip install conan~=$CONAN_VER

      - name: Install Rust Toolchains for iOS
        run: |
          rustup target add aarch64-apple-ios

      - name: Bootstrap Conan
        working-directory: TrustTunnelClient
        run: python ./scripts/bootstrap_conan_deps.py

      - name: Configure and Build Bridge (iOS)
        working-directory: TrustTunnelClient/platform/windows
        shell: bash
        run: |
          # 1. Inject warning suppressions
          sed -i '' 's/-Wall -Wextra -Wpedantic/-Wno-gnu-zero-variadic-macro-arguments/g' CMakeLists.txt
          
          # 2. Convert our bridge from SHARED to STATIC (using macOS BSD sed syntax)
          sed -i '' 's/SHARED/STATIC/g' ../../../go_client/trusttunnel/internal/dobby_bridge/CMakeLists.txt
          
          # 3. Inject into their tree
          echo 'add_subdirectory("../../../go_client/trusttunnel/internal/dobby_bridge" "dobby_bridge_build")' >> CMakeLists.txt
          
          mkdir build && cd build
          
          # 4. Compile using the Apple iOS Toolchain
          cmake -DCMAKE_SYSTEM_NAME=iOS \
            -DCMAKE_OSX_ARCHITECTURES=arm64 \
            -DCMAKE_OSX_DEPLOYMENT_TARGET=15.0 \
            -DCMAKE_BUILD_TYPE=RelWithDebInfo \
            -G "Ninja" ..
            
          cmake --build . --target dobby_bridge

      - name: Upload iOS Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dobby_bridge-ios-arm64
          path: TrustTunnelClient/platform/windows/build/dobby_bridge_build/libdobby_bridge.a